{
  "name": "react-wrapped-input",
  "version": "1.7.4",
  "description": "Wrapped inputs that integrate set and get functions to simplify building React forms.",
  "main": "./build/index.js",
  "repository": "https://github.com/hughbe/react-wrapped-input",
  "author": "Hugh Bellamy",
  "license": "MIT",
  "dependencies": {
    "react": "^16.5.2"
  },
  "devDependencies": {
    "@babel/cli": "^7.2.0",
    "@babel/core": "^7.2.0",
    "@babel/preset-react": "^7.0.0",
    "@babel/plugin-proposal-class-properties": "^7.2.3"
  },
  "scripts": {
    "build": "babel lib --presets @babel/preset-react --plugins @babel/plugin-proposal-class-properties --out-dir build",
    "prepublishOnly": "npm run build"
  }
}
